/** Host loader entry for the remote-control pairing tab browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the pairing tab. */
export function apply(): void {}
